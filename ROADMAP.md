# 路线图

这份文件说明**接下来会做什么、什么明确不做**，以及判断标准。
没有承诺时间表：这是一个单人维护的业余项目，优先级按「会不会伤到用户数据 / 会不会挡住贡献者」排。

## 正在进行（2.0.x）

| 事项 | 状态 | 说明 |
|------|------|------|
| 代码签名 | 管线就绪，等证书 | `CSC_LINK` / Azure Artifact Signing / `build/check-signature.ps1` / CI 验签全部接好。**路线已查证（2026-09）**：免费的有 Microsoft Store（MSIX 提交，商店用微软证书重签、开发者账号免注册费）与 SignPath Foundation（开源项目免费签名，但明说不给「没人知道的源码」签，需要项目有可验证声誉）；付费的可选 SSL.com IV（$129/年 + eSigner 订阅）或 Certum 开源证书（需物理卡）；Azure 对个人限美/加、大陆个人不可用。详见 [docs/release.md](docs/release.md#代码签名) |
| 首次公开发布收口 | 进行中 | 中文/英文 README 与 `docs/github-setup.md` 已就位；剩下的是仓库描述、topics、分支保护与 `OWNER` 占位符替换（照 [docs/github-setup.md](docs/github-setup.md) 走一遍） |

## 计划中（2.1）

| 事项 | 为什么值得做 | 需要的改动 |
|------|--------------|------------|
| 中断分析的细粒度视图 | 数据早就落盘了（`sisy-timer-history` 记了每轮的结果/暂停次数/时段），但只做了周月汇总，还没有「我经常在几点被打断」 | 纯渲染端 + `src/shared/statistics.js` 加汇总函数，补回归断言 |
| 统计结果导出 | 现在只能导出整库 JSON；想拿去做自己的报表不方便 | 本地生成 CSV，无网络，复用现有导出授权路径 |
| 演示 GIF | README 只有静态截图，看不出字符海在动 | 需要一个截图/合成工具（本机无 ffmpeg），倾向加一个 devDep 离线生成 |

## 候选（讨论后再定，未承诺）

- 任务标签 / 分组与筛选 —— 任务多了以后列表会变长，但会给数据模型加维度，要先想清楚迁移
- 历史保留策略可配置（当前硬上限 300 条）—— 与「统计要能跨月看」有张力
- 专注钟的窗口透明度 / 更好的多显示器记忆 —— 属于打磨，优先级低于正确性
- 中文之外的 UI 语言 —— 欢迎 PR，但需要先约定文案组织方式（见 [CONTRIBUTING.md](CONTRIBUTING.md)）

## 明确不做

这些不是「还没做」，而是**有意不做**，附理由：

| 不做 | 理由 |
|------|------|
| 账号、云同步、多设备 | 「数据就是你磁盘上的一个 JSON」是核心卖点；一旦有账号就要处理注册、找回、隐私合规和服务器成本 |
| 遥测、崩溃上报、自动更新检查 | 零网络请求是写进 [SECURITY.md](SECURITY.md) 的承诺，页面 CSP 是 `default-src 'none'` |
| 阻止系统睡眠 | Windows 上无法可靠承诺：合盖、手动睡眠、休眠都拦不住。与其做半个功能，不如如实写在 README「已知限制」里 |
| macOS / Linux 打包 | 自绘标题栏、托盘、电源事件全部按 Windows 调过，没有真机验证就不发产物 |
| 移动端 | 与「桌面专注工具」的定位冲突 |
| 引入框架 / 打包器 / 运行时依赖 | 现在的零运行时依赖 + 无打包步骤让「clone → npm start」就能跑，也让安全面容易讲清楚 |
| 云 AI 功能（自动拆任务、智能排期等） | 与本地优先直接冲突；真要接也得是本地模型，且不能成为默认路径 |

## 怎么提需求

在 issue 里说清**你想解决的问题**（而不是你想要的功能），并说明它属于上表的哪一类。
如果它命中「明确不做」，回复会直接引用这一节，而不是拖着。

> 本文的每一项判断标准都一样：**会不会伤到用户数据、会不会挡住贡献者、能不能诚实地讲清楚它做什么。**
