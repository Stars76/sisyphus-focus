# ---------------------------------------------------------------------------
# 依赖镜像（本地加速用，可选）
#
# 默认指向国内 npmmirror 公共镜像，本机拉依赖更快。三个地址都只是「下载地址」，
# 不改变包内容：package-lock.json 里记录了每个包的 integrity 哈希，npm 会逐包校验。
#
# 想改回官方源，任选其一（CI 里就是这么做的，见 .github/workflows）：
#   npm ci --registry=https://registry.npmjs.org
#   npm install --registry=https://registry.npmjs.org
#   $env:ELECTRON_MIRROR=''; npm install   # 让 Electron 二进制走官方 GitHub Releases
#
# 说明：lock 里的 resolved 一律写成官方 https://registry.npmjs.org（npm >= 9 的
# replace-registry-host=npmjs 会在安装时自动替换成这里配置的 registry），
# 因此换源只需要改这个文件，不用重新生成 lock。
# ---------------------------------------------------------------------------
registry=https://registry.npmmirror.com
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
