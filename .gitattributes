# 统一为 LF：源码与文档一律 LF，避免 Windows 贡献者（autocrlf=true）提交 CRLF，
# 让 diff 与打包产物在不同机器上保持一致。
* text=auto eol=lf

# 二进制（不做换行转换）
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.exe binary
*.zip binary
*.asar binary
